{
  "name": "delivery-fullstack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=prod npm run start --workspace=server",
    "client:dev": "npm run dev --workspace=client",
    "server:dev": "npm run dev --workspace=server",
    "server:prod": "npm run prod --workspace=server",
    "server:tables:create": "npm run tables:create --workspace=server",
    "build:client": "rm -rf apps/backend/dist && npm run build --workspace=client && cp -r apps/frontend/dist apps/backend/dist"
  },
  "workspaces": [ "apps/*" ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
