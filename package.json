{
  "name": "delivery-fullstack",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "client:dev": "npm run dev --workspace=client",
    "server:tables:create": "npm run tables:create --workspace=server",
    "server:dev": "npm run dev --workspace=server",
    "build": "rm -rf apps/backend/dist && npm run build --workspace=client && cp -r apps/frontend/dist apps/backend/dist"
  },
  "workspaces": [ "apps/*" ],
  "keywords": [],
  "author": "",
  "license": "ISC"
}
